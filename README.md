Le code est dans le dossier notebooks. 
notebooks/tiny_nerf.ipynb est le code d'origine, notebooks/tiny_nerf_pytorch notre code. 

Pour exécuter notre code, lancer les celulles de tiny_nerf_pytorch dans l'ordre, et changer la variable dataset pour lancer le modèle sur un autre dataset (contenu dans nerf_synthetic). Changer également le nombre d'itérations si voulu (par défaut, 10000).

Les dossiers nerf et nerf-pytorch sont là en tant que référence de notre travail sur lequel on se base. 

La présentation est dans diapo.pdf